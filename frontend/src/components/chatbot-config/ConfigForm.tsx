import React from "react";

// Minimal stub for chatbot config form
export function ConfigForm() {
  return (
    <form>
      <h2>Chatbot RAG Configuration</h2>
      <p>
        This is a stub. Expected fields: embedding model, chunking strategy,
        retrieval mode, reranking, LLM params, visual processing.
      </p>
    </form>
  );
}
